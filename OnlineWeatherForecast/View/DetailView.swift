//
//  DetailView.swift
//  OnlineWeatherForecast
//
//  Created by Abdussamed on 8.08.2021.
//

import SwiftUI

//struct DetailView: View {
    
    
    
    
    /*var body: some View {
        
        VStack{
            Group{
                CustomImageView().padding()//.offset(y: UIScreen.main.bounds.height * -0.1)
                
                Text(contentViewModel.weather?.weatherDescription ?? "")
                    .font(.title)
                    .foregroundColor(Color("customBlue"))
                    .padding(2)
                
                Text(contentViewModel.weather?.temparature ?? "")
                    .font(.largeTitle)
                    .bold()
                    .padding()
                
                Text(contentViewModel.weather?.country ?? "")
                    .font(.title)
                
                Text(contentViewModel.weather?.localTime ?? "").padding(20)
            }
            
            VStack{
                
                HStack{
                    
                    VStack{
                        
                        VStack{
                            Text("Feels Like")
                            Text(contentViewModel.weather?.feelsLike ?? "")
                        }.padding()
                            
                        VStack{
                            Text("Wind Speed")
                            Text(contentViewModel.weather?.windSpeed ?? "")
                        }.padding()
   
                    }.frame(width: UIScreen.main.bounds.width * 0.4, alignment:.center).padding()
                    
                    VStack{
                        
                        VStack{
                            Text("Humidity")
                            Text(contentViewModel.weather?.humidity ?? "")
                        }.padding()
                            
                        VStack{
                            Text("Pressure")
                            Text(contentViewModel.weather?.pressure ?? "")
                        }.padding()

                    }.frame(width: UIScreen.main.bounds.width * 0.4, alignment:.center).padding()
                    
                }
                
                VStack{
                    Text("Observation Time")
                    Text(contentViewModel.weather?.observationTime ?? "")
                }.padding()
                
            }
            
        }.frame(width: UIScreen.main.bounds.width,
                height: UIScreen.main.bounds.height,
                alignment: .center)
    }
}*/

/*struct DetailView_Previews: PreviewProvider {
    static var previews: some View {
        DetailView()
    }
}*/
